[project]
name = "db"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Shivang Rathore", email = "rshivang12345@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "configs",
    "sqlalchemy>=2.0.43",
]

[build-system]
requires = ["uv_build>=0.8.3,<0.9.0"]
build-backend = "uv_build"

[tool.uv.sources]
configs = { workspace = true }

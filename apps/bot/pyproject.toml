[project]
name = "bot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "db",
    "discord-py>=2.6.2",
]

[tool.uv.sources]
db = { workspace = true }
